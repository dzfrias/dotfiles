PATH="/usr/local/bin" 
 
# Every first minute of every day
1 0 * * * '/Users/diegofrias/.dotfiles/zsh/cron/task_overdue.py' 
# Delete all files and directories in the ~/.temporary dir
@daily /bin/rm -r ~/.temporary/*
